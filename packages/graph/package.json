{
  "name": "@tahu/graph",
  "version": "0.1.0",
  "private": true,
  "description": "Knowledge Graph — entity extraction, relations, query",
  "main": "./src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit", "test": "vitest run" },
  "dependencies": { "@tahu/core": "workspace:*" },
  "devDependencies": { "typescript": "^5.5.0", "vitest": "^2.0.0" }
}
