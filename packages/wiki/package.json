{
  "name": "@tahu/wiki",
  "version": "0.1.0",
  "private": true,
  "description": "Wiki Engine — generation, versioning, knowledge candidate pipeline",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tahu/core": "workspace:*",
    "@tahu/parser": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
