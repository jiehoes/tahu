{
  "name": "@tahu/parser",
  "version": "0.1.0",
  "private": true,
  "description": "Document parser — extract text from PDF, DOCX, HTML, TXT",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tahu/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
